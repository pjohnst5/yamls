apiVersion: v1
kind: Pod
metadata:
  name: windows-pod2
spec:
  containers:
    - name: windows-container2
      image: mcr.microsoft.com/dotnet/framework/samples:aspnetapp
      command: ["powershell", "Start-Sleep", "-s", "3600"]
  nodeSelector:
    kubernetes.io/os: windows
